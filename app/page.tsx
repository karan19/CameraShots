import Scene from "../components/Scene";

export default function Page() {
  return (
    <main className="relative h-screen w-full overflow-hidden bg-gradient-to-b from-ink via-graphite to-black">
      <Scene />
    </main>
  );
}
